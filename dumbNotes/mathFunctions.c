#include <stdio.h>
#include <math.h>

int main(){

    float x = 9.3;

    x = sqrt(x);

    x = pow(9, 2);

    x = round(x);

    x = ceil(x); //rounded up

    x = floor(x); // rounded down

    printf("%d\n", x);


    return 0;
}